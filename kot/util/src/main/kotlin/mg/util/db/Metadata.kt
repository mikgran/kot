package mg.util.db

class Metadata(fieldCount: Int = 0, name: String = "", uid: String = "") {
    val fieldCount: Int = fieldCount // TOIMPROVE: remove?
    val name: String = name
    val uid: String = uid
}
